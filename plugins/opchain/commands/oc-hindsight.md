---
description: Source-grounded operational memory with signed promotion and read-time validation.
---

Invoke the `oc-hindsight` skill with the user's arguments in `$ARGUMENTS`.
Preparation is not activation; preserve its explicit reviewer approval boundary.
