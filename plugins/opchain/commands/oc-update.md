---
description: Update repo-local Opchain skills while preserving telemetry and checkpoints
---

Invoke the `oc-update` skill. Run `/oc-update` with the user's arguments in
`$ARGUMENTS`; use its read-only check mode when the user asks to check only.
