---
description: Plan, draft, bump, and ship a versioned release
---

Invoke the `oc-release-ops` skill.

Before proposing a version: enumerate every tag, and reconcile the changelog
against them. A release that shipped without a changelog entry, or a changelog
entry with no tag, is a broken ledger — report it rather than continuing past it.
